function soma(x: number, y?: any): number {
    return x + y;
  }
  
  // Alternativa a
  console.log(soma(1, 2))
  
  // Alternativa b
  console.log(soma(1, "2"))
  
  // Alternativa c
  console.log(soma(1))